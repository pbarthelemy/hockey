version: '3.8'

services:
  scoresheet-app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      # Persist CSV and HTML data
      - ./csv:/app/csv
      - ./html:/app/html
    environment:
      - NODE_ENV=production
    restart: unless-stopped
